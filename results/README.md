# Recorded results

The figures in `figures/` were generated from individual experimental runs.
They are included to document training behavior, not as cross-validated
benchmark results.

The English figures can be regenerated from the scripts in `scripts/plots/`.
Earlier Chinese-language exports are retained under `figures/legacy/`.
